on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-publish:
    uses: jverneuer/browsercore-dev/.github/workflows/reusable-ci.yml@ci-v1
    with:
      node-version: '26'
