on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-publish:
    uses: jverneuer/browsercore-dev/.github/workflows/ci.yml@main
    with:
      node-version: '26'
    secrets: inherit
